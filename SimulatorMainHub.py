import math
import random

def main():
  def currentEvent  # will be the class of the next upcoming event in the list
  def currentEventTime  # will be the timestamp of the event with the lowest value timestamp
  def nextGCD       # will be the timestamp of the next upcoming GCD
  
  if currentEvent type.delayable = true # checks for non-delayable events, suc
